This a CRUD Application built with Node, Express, MongoDB and Pug Template.

It allows users to post, comment on and manage articles in different categories. 

--In order to run the app, install the dependencies first with...

npm install

--Then run the app with

npm start

//OR if you have nodemon installed (npm install -g nodemon)
nodemon